/**
 * Registry types — re-exported from the canonical types module.
 *
 * Import from here when working within the registry subsystem.
 */

export type {
  EnvPrompt,
  McpServerConfig,
  McpServerTemplate,
} from "../types.js";
